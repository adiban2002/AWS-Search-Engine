version: '3.8'

services:
  api:
    build: .
    ports:
      - "8002:8002"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - AWS_REGION=ap-south-1
      - PYTHONPATH=/app/backend
    restart: always